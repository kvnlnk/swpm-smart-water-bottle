name: swpm_flutter_app
description: "Smart Water Bottle Flutter App"
publish_to: 'none' 

version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  flutter_dotenv: ^5.2.0
  supabase_flutter: ^2.0.0
  supabase_auth_ui: ^0.5.5
  http: ^1.0.0
  intl: ^0.18.1
  provider: ^6.1.1 #store
  flutter_blue_plus: ^1.32.7
  permission_handler: ^11.3.1
  shared_preferences: ^2.5.3
  loading_animation_widget:
  

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^5.0.0


flutter:
  uses-material-design: true
  assets:
    - .env

